{
  "name": "dynamic-denon-tuning",
  "version": "0.4.0",
  "description": "Codex-controlled, evidence-gated auto-tuning orchestrator for Denon Atmos theaters",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "check": "find src test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "3.25.76"
  }
}
